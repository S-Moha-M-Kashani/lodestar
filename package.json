{
  "name": "lodestar",
  "version": "1.7.4",
  "description": "Lodestar — your compass for life: capture every open question, prioritize what matters, and never lose one. SQLite-backed so nothing is lost.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test-board": "PORT=3001 BOARD_DB=databases/test/board-3001.db ASSISTANT_DB=databases/test/assistant-3001.db AGENT_URL=http://127.0.0.1:9001 node server.js",
    "test-brain": "BOARD_API_URL=http://127.0.0.1:3001 BRAIN_CHROMA_URL=http://localhost:8004 BRAIN_CHROMA_DATABASE=lodestar-test BRAIN_CHECKPOINT_DB=databases/test/brain-checkpoints-3001.db uv run --project brain --extra local-embeddings uvicorn lodestar_brain.server:app --port 9001",
    "chroma": "docker compose up -d chroma",
    "chroma-test": "docker compose up -d chroma-test",
    "backup": "node scripts/backup-db.mjs",
    "postgres:schema": "docker exec -i ${LODESTAR_PG_CONTAINER:-postgres} psql \"${LODESTAR_PG_URL:?set LODESTAR_PG_URL to the lodestar connection string}\" -v ON_ERROR_STOP=1 -f - < scripts/postgres/001-schema.sql",
    "test:server": "npm ci --prefer-offline --no-audit --fund=false && node scripts/backup-db.mjs && node --test tests/*.test.js",
    "test:all": "npm ci --prefer-offline --no-audit --fund=false && node scripts/backup-db.mjs && node --test tests/*.test.js && uv run --project brain pytest brain/tests -v && uv run --with playwright python tests/e2e_test.py",
    "eval-live": "BRAIN_EVAL_LIVE=1 BRAIN_LLM=claude-cli uv run --project brain pytest brain/tests/evals -m live -v",
    "hooks": "node scripts/install-hooks.mjs",
    "hygiene": "node scripts/release-hygiene.mjs master $(git tag --list 'v*' --merged master)",
    "release": "node scripts/release-to-master.mjs",
    "auth:setup": "node scripts/set-password.mjs"
  },
  "engines": {
    "node": ">=23.4.0"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
